// export const updatingProfessionCard = (actionValue, state) => {
	// const { data, objKeyList } = actionValue;
	
	// const rootObj = state[objKeyList[0]];			
	// const childObj = rootObj[objKeyList[1]];
	
	// // const res = childObj.map(item => 
		// // item.id === data.id 
			// // ? { ...item, [data.name]: data.value } 
			// // : item
	// // );
	// const res = [];
	
	// return { rootObj, res };
// };